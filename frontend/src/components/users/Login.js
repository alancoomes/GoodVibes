import React from "react";

export default function Login() {
  return (
    <form onSubmit={}>
      <input type="text" onChange={} name="username" value={}></input>
      <input type="text" onChange={} name="password" value={}></input>
      <input type="submit" value="Login" />
    </form>
  );
}
